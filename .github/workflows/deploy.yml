name: Deploy

on:
  push:
    branches:
      - main

concurrency: deploy

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
